spring.application.name=backend

# 1. db 설정
# Oracle Cloud DB 연결 설정 (Wallet 사용)
# wallet 파일들은 src/main/resources/wallet 폴더에 저장하세요
# wallet 폴더에는 다음 파일들이 있어야 합니다:
# - tnsnames.ora
# - sqlnet.ora
# - wallet 파일들 (cwallet.sso, ewallet.p12 등)

# TNS_ADMIN 경로 설정 (wallet 파일 위치)
# Windows 환경에서는 절대 경로 또는 상대 경로 사용 가능
spring.datasource.url=jdbc:oracle:thin:@jc4dxlbowsuduo56_high
spring.datasource.username=ADMIN
spring.datasource.password=@Kjy09040423
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# HikariCP 연결 풀 설정 (세션 관리)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.leak-detection-threshold=60000

# Oracle Wallet을 위한 시스템 프로퍼티 설정
# TNS_ADMIN은 wallet 폴더의 절대 경로로 설정
# 예: C:/danseongsa/danseongsa/backend/backend/src/main/resources/wallet
# 또는 상대 경로: classpath:wallet (런타임에 자동으로 해석됨)

# 2. jpa 설정
spring.jpa.hibernate.ddl-auto=update
spring.sql.init.mode=never
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect
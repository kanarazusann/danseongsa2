spring.application.name=backend

# 서버 포트 설정 (Elastic Beanstalk는 포트 5000 사용)
server.port=${SERVER_PORT:8080}

# 1. db 설정 (환경 변수 사용)
spring.datasource.url=${DB_URL:jdbc:oracle:thin:@jc4dxlbowsuduo56_high}
spring.datasource.username=${DB_USERNAME:ADMIN}
spring.datasource.password=${DB_PASSWORD:@Kjy09040423}
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# HikariCP 
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.leak-detection-threshold=60000

# Oracle Wallet

# 2. jpa 설정
spring.jpa.hibernate.ddl-auto=validate
spring.sql.init.mode=never
spring.jpa.show-sql=${JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect

# 3. mail 설정 (Brevo SMTP)
spring.mail.host=${MAIL_HOST:smtp-relay.brevo.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:9cb08b001@smtp-brevo.com}
spring.mail.password=${MAIL_PASSWORD:AR43fBygTULnSW8k}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

mail.verification.from=${MAIL_FROM:no-reply@danseongsa.com}
spring.mail.default-encoding=UTF-8
mail.verification.subject=\u005B\ub2e8\uc131\uc0ac\u005D \uc774\uba54\uc77c \uc778\uc99d \ucf54\ub4dc
mail.verification.expire-minutes=5

# Toss Payments (환경 변수 사용)
toss.payments.secret-key=${TOSS_SECRET_KEY:test_sk_eqRGgYO1r5goK2PqOPg43QnN2Eya}

# CORS 설정 (환경 변수 사용)
cors.allowed.origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}

server.servlet.session.cookie.same-site=none
server.servlet.session.cookie.secure=true



aws.region=${AWS_REGION:ap-northeast-2}
aws.access-key-id=${AWS_ACCESS_KEY_ID:}
aws.secret-access-key=${AWS_SECRET_ACCESS_KEY:}
aws.s3.bucket=${AWS_S3_BUCKET:}
aws.s3.base-url=${AWS_S3_BASE_URL:}